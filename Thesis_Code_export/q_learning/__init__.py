"""Tabular Q-learning for hot coffee roasting (single-shift PSC restock + MTO).

Public modules:
- ``train``: training loop + state discretization (QStrategy).
- ``evaluate``: single-seed evaluation + HTML report rendering.
"""
